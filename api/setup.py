from setuptools import setup

setup(
   name='api',
   version='1.0.0',
   description='api',
   author='',
   author_email='',
   packages=['api'],
   install_requires=[],
   scripts=[
           ]
)